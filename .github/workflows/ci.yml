name: File 22 CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

concurrency:
  group: file-22-ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  php-lint:
    name: PHP ${{ matrix.php }} syntax
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        php: ['8.1', '8.2', '8.3']
    steps:
      - name: Check out repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: Set up PHP
        uses: shivammathur/setup-php@7c071dfe9dc99bdf297fa79cb49ea005b9fcadbc # v2.37.1
        with:
          php-version: ${{ matrix.php }}
          coverage: none
      - name: Lint PHP files
        shell: bash
        run: |
          set -euo pipefail
          mapfile -d '' files < <(find . -type f -name '*.php' -not -path './vendor/*' -print0)
          if [ "${#files[@]}" -eq 0 ]; then
            echo 'No PHP files found.' >&2
            exit 1
          fi
          for file in "${files[@]}"; do
            php -l "$file"
          done

  phpcs:
    name: WordPress security standards
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: shivammathur/setup-php@7c071dfe9dc99bdf297fa79cb49ea005b9fcadbc # v2.37.1
        with:
          php-version: '8.3'
          coverage: none
          tools: composer:v2
      - run: composer install --no-interaction --no-progress --prefer-dist --quiet
      - run: composer phpcs

  phpstan:
    name: PHPStan
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: shivammathur/setup-php@7c071dfe9dc99bdf297fa79cb49ea005b9fcadbc # v2.37.1
        with:
          php-version: '8.3'
          coverage: none
          tools: composer:v2
      - run: composer install --no-interaction --no-progress --prefer-dist --quiet
      - name: Analyze and capture diagnostics
        id: analyze
        shell: bash
        run: |
          set +e
          vendor/bin/phpstan analyse --configuration=phpstan.neon.dist --error-format=json --no-progress > phpstan-result.json
          status=$?
          echo "status=$status" >> "$GITHUB_OUTPUT"
          cat phpstan-result.json
          exit 0
      - name: Upload PHPStan diagnostics
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: phpstan-result
          path: phpstan-result.json
          if-no-files-found: error
          retention-days: 3
      - name: Enforce PHPStan result
        shell: bash
        run: test "${{ steps.analyze.outputs.status }}" = "0"

  phpunit:
    name: PHPUnit contract tests
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: shivammathur/setup-php@7c071dfe9dc99bdf297fa79cb49ea005b9fcadbc # v2.37.1
        with:
          php-version: '8.3'
          coverage: none
          tools: composer:v2
      - run: composer install --no-interaction --no-progress --prefer-dist --quiet
      - run: composer test

  repository-contract:
    name: Repository contract
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Check out repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - name: Validate Composer configuration
        run: composer validate --strict --no-check-lock
      - name: Verify required files and identifiers
        shell: bash
        run: |
          set -euo pipefail
          required=(
            README.md LICENSE.md CHANGELOG.md MANIFEST.md .distignore
            composer.json phpunit.xml.dist phpstan.neon.dist phpcs.xml.dist
            sabri-universal-post-composer.php
            assets/css/create-surface.css
            includes/contracts/interface-adapter.php
            includes/contracts/interface-workflow-adapter.php
            includes/contracts/interface-diagnostic-adapter.php
            includes/core/class-permission-resolver.php
            includes/core/class-page-resolver.php
            includes/core/class-safe-mode.php
            includes/core/class-plugin.php
            includes/core/class-registry.php
            includes/core/class-workflow-coordinator.php
            includes/core/functions.php
            includes/presentation/class-create-surface.php
            includes/integration/class-shell-bridge.php
            includes/integration/class-core-adapter-requirements.php
            includes/admin/class-system-check-page.php
            tests/CreateSurfaceTest.php
            tests/RegistryTest.php
            tests/AccessibilityContrastTest.php
            tests/AdminSystemCheckTest.php
            tests/PageResolverRepairTest.php
            tests/WorkflowCoordinatorTest.php
            tests/phpstan-wordpress-stubs.php
            docs/ARCHITECTURE.md docs/ADAPTER-CONTRACT.md docs/DECISION-LOG.md docs/SECURITY.md
            docs/PRIVACY.md docs/ACCESSIBILITY.md docs/MIGRATION.md docs/ROLLBACK.md
            docs/STAGING-ACCEPTANCE.md docs/COMPATIBILITY-MATRIX.md docs/ERROR-CODES.md
            docs/FILE21-INTEGRATION.md docs/MASTER-PLAN-AMENDMENT-v2.1.md
            docs/POST-IMPLEMENTATION-REVIEW-2026-07-29.md
            docs/PHASE-22C-CREATE-SURFACE.md
            docs/PHASE-22C-POST-IMPLEMENTATION-REVIEW-2026-07-29.md
            docs/PHASE-22C-SECOND-REVIEW-CORRECTIONS-2026-07-29.md
            docs/PHASE-22C-POST-CORRECTION-VERIFICATION-2026-07-29.md
            docs/PHASE-22D-ADMIN-SYSTEM-CHECK.md
            docs/PHASE-22D-POST-IMPLEMENTATION-REVIEW-2026-07-29.md
            docs/PHASE-22D-POST-CORRECTION-VERIFICATION-2026-07-29.md
            docs/PHASE-22D-SECOND-REVIEW-CORRECTIONS-2026-07-29.md
            docs/PHASE-22D-SECOND-POST-CORRECTION-VERIFICATION-2026-07-29.md
            docs/PHASE-22E-WORKFLOW-ORCHESTRATION.md
            docs/PHASE-22E-POST-IMPLEMENTATION-REVIEW-2026-07-29.md
            docs/PHASE-22E-POST-CORRECTION-VERIFICATION-2026-07-29.md
          )
          for file in "${required[@]}"; do
            test -f "$file" || { echo "Missing required file: $file" >&2; exit 1; }
          done
          grep -q 'Plugin Name: Sabri Universal Post Composer' sabri-universal-post-composer.php
          grep -q "define( 'SUPC_ADAPTER_API_VERSION'" sabri-universal-post-composer.php
          grep -q "define( 'SUPC_WORKFLOW_API_VERSION'" sabri-universal-post-composer.php
          grep -q 'One canonical native record' README.md
          grep -q 'social_publication' docs/FILE21-INTEGRATION.md
          grep -q 'Post-Implementation Review' docs/POST-IMPLEMENTATION-REVIEW-2026-07-29.md
          grep -q 'File 23' docs/MASTER-PLAN-AMENDMENT-v2.1.md
          grep -q 'validate_internal_route' includes/presentation/class-create-surface.php
          grep -q 'invalid_privacy' includes/presentation/class-create-surface.php
          grep -q 'create_surface_diagnostics' includes/presentation/class-create-surface.php
          grep -q 'creation_state_for_user' includes/core/class-registry.php
          grep -q 'One gateway, one native record.' includes/presentation/class-create-surface.php
          grep -q 'background: var(--supc-orange-dark);' assets/css/create-surface.css
          grep -q 'prefers-reduced-motion' assets/css/create-surface.css
          grep -q 'forced-colors' assets/css/create-surface.css
          grep -q 'Phase 22C Post-Implementation Review' docs/PHASE-22C-POST-IMPLEMENTATION-REVIEW-2026-07-29.md
          grep -q 'Second Review Corrections' docs/PHASE-22C-SECOND-REVIEW-CORRECTIONS-2026-07-29.md
          grep -q 'Post-Correction Verification' docs/PHASE-22C-POST-CORRECTION-VERIFICATION-2026-07-29.md
          grep -q 'Tools → Composer Health' docs/PHASE-22D-ADMIN-SYSTEM-CHECK.md
          grep -q 'Phase 22D Post-Implementation Review' docs/PHASE-22D-POST-IMPLEMENTATION-REVIEW-2026-07-29.md
          grep -q 'Phase 22D Second Review Corrections' docs/PHASE-22D-SECOND-REVIEW-CORRECTIONS-2026-07-29.md
          grep -q 'Phase 22D Second Post-Correction Verification' docs/PHASE-22D-SECOND-POST-CORRECTION-VERIFICATION-2026-07-29.md
          grep -q 'mapping_persistence_failed' includes/core/class-page-resolver.php
          grep -q 'ambiguous_selection_required' includes/core/class-page-resolver.php
          grep -q 'REPAIR_LOCK_OPTION' includes/core/class-page-resolver.php
          grep -q 'scope="col"' includes/admin/class-system-check-page.php
          grep -q 'Static Adapter Health' includes/admin/class-system-check-page.php
          grep -q 'workflow_api_version' includes/contracts/interface-workflow-adapter.php
          grep -q 'class Workflow_Coordinator' includes/core/class-workflow-coordinator.php
          grep -q 'native_drafts_unsupported' includes/core/class-workflow-coordinator.php
          grep -q 'supc_native_workflow_error' includes/core/class-workflow-coordinator.php
          grep -q 'IDEMPOTENCY_PATTERN' includes/core/class-workflow-coordinator.php
          grep -q 'MAX_PREVIEW_TTL' includes/core/class-workflow-coordinator.php
          grep -q 'supc_workflow_submit' includes/core/functions.php
          grep -q 'Guarded Native Workflow Orchestration Foundation' docs/PHASE-22E-WORKFLOW-ORCHESTRATION.md
          grep -q 'Phase 22E Post-Implementation Review' docs/PHASE-22E-POST-IMPLEMENTATION-REVIEW-2026-07-29.md
          grep -q 'Phase 22E Post-Correction Verification' docs/PHASE-22E-POST-CORRECTION-VERIFICATION-2026-07-29.md
          grep -q 'includes/core/class-workflow-coordinator.php' MANIFEST.md
          grep -q 'docs/PHASE-22E-POST-IMPLEMENTATION-REVIEW-2026-07-29.md' MANIFEST.md
          grep -q 'docs/PHASE-22E-POST-CORRECTION-VERIFICATION-2026-07-29.md' MANIFEST.md
